<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FriendBook | Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    body {
      background-color: #f0f2f5;
    }

    .sidebar {
      background: #fff;
      border-radius: 8px;
      padding: 20px;
      height: 100vh;
      position: sticky;
      top: 0;
    }

    .navbar-brand img {
      height: 40px;
    }

    .post-box textarea {
      resize: none;
    }

    .post-box,
    .profile-update {
      background: #fff;
      padding: 20px;
      border-radius: 8px;
    }

    .card {
      border-radius: 8px;
    }
        input.form-control:focus {
            box-shadow: 0 0 0 0.2rem rgba(13, 110, 253, 0.25);
        }

        input.form-control,
        textarea.form-control {
            font-family: 'Poppins', sans-serif;
        }

        input.form-control:focus,
        textarea.form-control:focus {
            outline: none;
            box-shadow: none;
            border-color: #ced4da;
        }    
  </style>
</head>

<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand d-flex align-items-center gap-2" href="/home">
        <i class="fa fa-users text-primary fs-4"></i>
        <strong>FriendBook</strong>
      </a>

      <div class="collapse navbar-collapse">
        <ul class="navbar-nav mx-auto">
          <li class="nav-item">
            <a class="nav-link" href="/home"><i class="fa fa-home me-1"></i>Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/user/<%= user.username %>"><i class="fa fa-user me-1"></i>Profile</a>
          </li>
        </ul>
        <div class="dropdown">
          <a href="#" class="d-flex align-items-center text-dark text-decoration-none dropdown-toggle"
            data-bs-toggle="dropdown">
            <img src="<%= user.profilePic %>" class="rounded-circle" width="32" height="32" />
            <span class="ms-2">
              <%= user.name %>
            </span>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="/user/<%= user.username %>"><i class="fa fa-user me-2"></i>View
                Profile</a></li>
            <li><a class="dropdown-item" href="/api/auth/logout"><i class="fa fa-sign-out-alt me-2"></i>Logout</a></li>
          </ul>
        </div>
      </div>
    </div>
  </nav>